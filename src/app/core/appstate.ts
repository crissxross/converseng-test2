import { Convoturn } from './convoturn.model';

export interface AppState {
  convo: Convoturn[];
  flowing: boolean; // not sure, trying this out ??
}

// In fact, not sure what I'm doing with this at all yet!
